repos:
  - repo: local
    hooks:
      - id: run-pytest
        name: Run pytest
        entry: pytest
        language: python
        types: [python]
      - id: run-vitest
        name: Run vitest
        entry: vitest
        language: node
        types: [file]
        files: \.(js|ts)$
